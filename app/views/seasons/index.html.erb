<h1><%=@film.title%></h1>
<%= link_to 'Create season', new_season_path, class: 'btn btn-primary' %>
<ul>
  <% @seasons.each do |season| %>
    <li>
      <h4><%= link_to "#{season.title}", season_path(season) %></h4>
    </li>
  <% end %>
</ul>
